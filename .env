PORT=3001

# API do VendaLume-MS (cookies de sessão). Com "proxy" no package.json, use caminho relativo:
REACT_APP_API_URL=/api

# Se preferir URL absoluta (configure CORS no backend para o origin do Garçom):
# REACT_APP_API_URL=http://localhost:8080/api
