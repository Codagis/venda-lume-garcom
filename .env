# Ver `.env.example` para documentação completa.

# Desativa o ESLint no webpack (evita falha do build no CI com CI=true).
DISABLE_ESLINT_PLUGIN=true

PORT=3001

# Base da API (incluir /api). Com proxy no package.json: caminho relativo.
REACT_APP_API_URL=/api
